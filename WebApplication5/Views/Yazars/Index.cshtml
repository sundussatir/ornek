@model IEnumerable<WebApplication5.Models.Yazar>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.YazarAdiSoyadi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.YazarDogumTarihi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.YazarAdiSoyadi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.YazarDogumTarihi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.YazarId }) |
            @Html.ActionLink("Details", "Details", new { id=item.YazarId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.YazarId })
        </td>
    </tr>
}

</table>
