@model IEnumerable<WebApplication5.Models.KitapTuru>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.KitapTuruAdi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.KitapTuruAciklamasi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.KitapTuruAdi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.KitapTuruAciklamasi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.KitapTuruId }) |
            @Html.ActionLink("Details", "Details", new { id=item.KitapTuruId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.KitapTuruId })
        </td>
    </tr>
}

</table>
